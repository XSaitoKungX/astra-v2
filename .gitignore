# ╔═══════════════════════════════════════════════════════════════╗
# ║  Astra v2 — Public Showcase Repository                        ║
# ║  Source code is proprietary. Only showcase files are public.  ║
# ║  Purchase the full bot at: https://store.novaplex.xyz         ║
# ╚═══════════════════════════════════════════════════════════════╝

# ── Source Code (HIDDEN) ──
src/
index.js
scripts/

# ── Dashboard (HIDDEN) ──
dashboard/src/
dashboard/index.html
dashboard/vite.config.js
dashboard/eslint.config.js
dashboard/tailwind.config.js
dashboard/postcss.config.js
dashboard/package.json
dashboard/package-lock.json
dashboard/bun.lock
dashboard/bun.lockb
dashboard/.gitignore
dashboard/README.md

# ── Build Output (HIDDEN) ──
dist/
dashboard/dist/
release/
release-fullsource/

# ── Dependencies (HIDDEN) ──
node_modules/
dashboard/node_modules/

# ── Environment (HIDDEN) ──
.env
.env.local

# ── Database (HIDDEN) ──
prisma/migrations/
prisma.config.ts

# ── Logs (HIDDEN) ──
logs/
*.log

# ── License cache (lifetime flag, machine-specific) ──
.license-cache/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Server Config (HIDDEN) ──
nginx/

# ── Misc ──
*.tgz
*.zip
.cache/
bun.lock
bun.lockb
package-lock.json
todo.md
